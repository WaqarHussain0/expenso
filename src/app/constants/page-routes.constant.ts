const PAGE_ROUTES = {
  // Auth Routes
  login: '/login',
  register: '/register',
  forgotPassword: 'forgot-password',

  // Authenticated Routes
  dashboard: '/dashboard',
  category: '/category',
  transaction: '/transaction',
};

export default PAGE_ROUTES;
