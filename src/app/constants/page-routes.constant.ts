const PAGE_ROUTES = {
  // Auth Routes
  login: "/login",

  // Authenticated Routes
  dashboard: "/dashboard",
  category: "/category",
  transaction: "/transaction",
};

export default PAGE_ROUTES;
